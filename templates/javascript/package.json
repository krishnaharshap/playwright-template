{
  "name": "playwright-enterprise-template-js",
  "version": "1.0.0",
  "description": "Enterprise Playwright Template - JavaScript Implementation",
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "allure-playwright": "^3.0.0",
    "dotenv": "^16.4.5"
  },
  "scripts": {
    "test": "npx playwright test",
    "test:smoke": "npx playwright test --grep @smoke",
    "report": "npx playwright show-report ./reporting/playwright-html"
  }
}
